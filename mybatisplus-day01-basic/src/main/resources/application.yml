spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.1.8:3306/db_mybatisplus?useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: xxxxx
logging:
  level:
    root: warn
    com.liuhang.mybatisplus.mybatisplusday01basic.dao: trace
  pattern:
    console: '%p%m%n'
mybatis-plus:
  mapper-locations: classpath*:/mapper/*.xml

  # mybatis 的配置文件
  config-location: classpath:mybatis-config.xml

  # 配置全局的主键生成策略
#  global-config:
#    db-config:
#      id-type:
